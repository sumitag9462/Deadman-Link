# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/deadman_link

# Server
PORT=4000
JWT_SECRET=changeme

# SMTP (for sending OTPs)
# If using Gmail, enable 2FA and create an App Password, then set SMTP_USER to your gmail address
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your.email@gmail.com
SMTP_PASS=your_app_password_here
FROM_EMAIL=no-reply@deadman-link.local

# Optional: When running in production behind a reverse proxy, set the public URL
APP_URL=http://localhost:5173
